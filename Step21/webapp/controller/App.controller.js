sap.ui.define([
   "sap/ui/core/mvc/Controller",
  
], function (Controller) {
   "use strict";
   return Controller.extend("sap.ui.demo.walkthrough.controller.App", {
  
   });
});

// App.controller.js 에 정의된 모든 것이 HelloPanel.controller.js로 옮겨져 정의된다. 따라서 빈 stub으로 남는다.