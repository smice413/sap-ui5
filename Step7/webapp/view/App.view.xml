<mvc:View
   controllerName="sap.ui.demo.walkthrough.controller.App"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc">
   <Button
      text="Say Hello"
      press=".onShowHello"/>
   <Input
      value="{/recipient/name}"
      description="Hello {/recipient/name}"
      valueLiveUpdate="true"
      width="60%"/>
</mvc:View>

<!-- 
   controllerName을 통해(이는 컨트롤러에서 정의한 이름이다.) 컨트롤러를 참조한다는 것을 설명하고, 이를 통해 컨트롤러에서 button태그에 있는 onShowHello가 트리거가 되어 함수가 실행되도록 한다.
   Input 태그를 추가하고 xml 뷰에 대한 선언적 바인딩 구문을 사용하여 값을 바인딩한다. 즉, 중괄호{}안에 모델의 경로를 선언하면 값을 바인딩할 수 있다.

-->